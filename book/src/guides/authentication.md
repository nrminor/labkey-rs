# Authentication

All four credential types with full examples: API key, basic auth (email/password), netrc file lookup, and guest access. When to use each, environment variable patterns for CI/CD, and the netrc file format for multi-server setups.
