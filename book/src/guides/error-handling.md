# Error Handling

The `LabkeyError` enum and when each variant occurs. Structured API errors with `ApiErrorBody`. The 200-with-embedded-exception pattern where LabKey returns HTTP 200 but the body contains an exception. API version errors. Writing match arms for error recovery and diagnostics.
