# Filters and Sorts

Building filters with `Filter::new` and `FilterType`. Multi-value filters (IN, NOT IN). The `display_text`/`from_name` round-trip for dynamic filter construction. `QuerySort::parse` for sort strings. How filters and sorts compose in query options.
