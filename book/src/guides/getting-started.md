# Getting Started

How to add labkey-rs to your project, construct a client, make your first query, and inspect the response. Covers dependency setup, API key authentication from environment variables, and the basic `select_rows` workflow.
