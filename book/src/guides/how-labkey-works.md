# How LabKey Works

A brief orientation to the LabKey concepts that matter when using this client: containers (folder paths), schemas and queries, and the controller/action URL structure. This page gives just enough context for the rest of the book to make sense, then links out to [LabKey's own documentation](https://www.labkey.org/Documentation/wiki-page.view?name=docs) for anything deeper.
