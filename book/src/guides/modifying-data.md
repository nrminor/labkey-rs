# Modifying Data

Writing data to a LabKey server. Covers `insert_rows`, `update_rows`, and `delete_rows` with `serde_json` row construction, `save_rows` for mixed operations in a single request, `import_data` for bulk import, and `truncate_table`. Discusses transactional behavior.
