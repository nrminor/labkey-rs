# Querying Data

Reading data from a LabKey server. Covers `select_rows` in depth (columns, container filters, `include_total_count`, response structure, the `CellValue` wrapper), `execute_sql` for LabKey SQL, `select_distinct_rows` for unique values, and `get_query_details` for schema introspection.
