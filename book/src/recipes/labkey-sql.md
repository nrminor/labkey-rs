# LabKey SQL

How to use `execute_sql` for queries that go beyond what `select_rows` supports. Covers LabKey SQL syntax differences from standard SQL, WAF encoding (handled automatically by the client), and when to prefer SQL over the query API.
